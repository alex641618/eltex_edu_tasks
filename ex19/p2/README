Для компиляции сервера выполните команду:
# make server

Для компиляции клиента выполните команду:
# make client

Или просто выполните команду
# make

Запустите клиент командой:
# ./client -a [server address] -p [port] -i <interface>

Запустите сервер командой:
# ./server -a <client address> -p [port] -i <interface>

Чтобы удалить бинарные файлы выполните команду:
# make clean

*[parameter] - обязательные аргументы
 <parameter> - необязательные аргументы

Примечание: Запускайте, сначала сервер на определенном хосте, а затем клиента. 
END - сообщение для завершение потока обслуживающего сервера
При получении SIGINT клиентом на сервер отправляется сообщение END и клиент завершается вместе с обслуживающим сервером,
если поток этого сервера не является частью начального пула серверов, главный сервер при этом продолжает работу.
Для генерации большого числа клиентов запустите скрипт client_gen.sh командой
# ./client_gen.sh [количество клиентов]
Скрипт запустится, при нажатии ENTER или Ctrl+C всем сгенерированным клиентам отправится сигнал SIGINT 
